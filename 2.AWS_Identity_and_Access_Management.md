

In bank 
1] First one you need to be an authenticated person to enter into the bank and you should have an account
2] Verify what authorization you have whether you are a bank employee or bank account holder or any superior person.. (If you have authorization to visit only service desk we will send you to that area only Okay).

In aws 
If we create the account we can get the root access we can do technically anything in the aws 
If the devops engineer create an account for business related give root access to all the employees present in the company they  have access to do anything into this aws account of the company right and mistakenly or knowningly delete some resources means we completely messed up with the aws account right.

AWS Came up with the authentication and authorization service concept.

IAM Has 4 concepts 
Users, Groups, Roles, Policies 

User : Authentication
You are creating a user account for the user xyz you are just allowing them to enter into the aws Okay

Policies : Authorization
What this user has to do 

Groups : 
To increase the efficiency of creating the user and policies because some of them will join organization and some of them are leaving organization so everytime creating user and setting policies it will reduce the efficiency in work thats why group is coming to play Okay..
like developers, OA Engineers, DB Admins and the rest of others 
(creating a group like above four) Just create the user and add to the groups so that 
IAM Service these are the things Okay 

---
# Creating Authentication :

Sign in to the console using the root account

IAM Service

STEP 1 : SPECIFY USER DETAILS :
```
Go to IAM Service

Click user and Click add user =  So We can add the user
	Provide the name
	Click the provide access to the creating user 
	I want to create a IAM user or Root User (now keep it as a iam user)
	Auto generated password Click 
	Click the user has to create the new password at next sign in Okay

Asking the user to click Next click that...
```

Step 2 : Set Permissions
```
Keep it as default and click next 
Without authorization they can enter into aws account but they cant do anything like delete the resources or create the resources
```

Step 3 :  Review and Create
```
User details

One policy should be present as the default to change the password permission that is the only policy they can use 

Then click on the create user
```

Step 4 : Retrieve Password
```
Here we can get the details about the password and we can download this as a .csv file Okay 

This file contains four columns username, Password, SignIn Console url Okay
1. Account ID present in the url in the format of 10 integer number Okay 
```

Then logout and click the sign-in console

In Login thing You have to Select IAM User instead of Root user and enter password 
It will open a page for resetting the password then reset the password will happen for this.

To check you mentioned the IAM User Correctly or not when you go for some services it will show some error as you don't have permissions to do this to list the bucket, to create the instance or launch the instance like that Okay.

Log in into Shopping Shop but can't order any or add to the checklist. 

---

Now you have to tell the Devops Engineer As you allowed me to the AWS Account, but whenever i log in I want to see the list of buckets present in the s3 services in the AWS, attach some policies called s3 to list all my buckets.

# Creating Authorization :

##### Attaching some policies to list the buckets in the s3 service Okay.
```
1.Go to the IAM Service

2.Go to users sections in the sidebar

3.Click the user you want to add some policies

4.Click on permissions Only Policy Defaultly Present is UserChangePasswordPolicy

5.Click on add permissions in the right side of the option 

6.Click on attach the policy directly (Some managed policies will list out 1110+ policies)

7.Click some policies you want to provide to the user Okay 

8.Click next and add permissions It will be added to the user policies(authorization) Okay
   
9. Again check with the log in of the user and mentioned access is accessible or not Okay.
```

---

Never use the root user in interview if they ask means tell when we create the aws account we have created the IAM user for DevOps team that we are using only not the root user Okay. We authorized with the Devops permission and actions.

--- 
###### Concept of user including into the groups 

```
1.Click on User Groups

2.Click on Create new group on the right side of the top 

3.Provide the groupname

4.Then we can select policies what are the groups members can do Okay.

5.Click on Create group

6.Go to the group we created and click user to see the user in this group

7.Click on add user and select and include Okay.

8.Add permission also present in this.
```

---

```
Go to IAM user,
Create the user,
Log in as the user,
Add the permission to the s3 access to this user, 
Check with accessible or not using the user we created, 
Then with groups create and add the user to this group and allocate the policies For the user in this group to do some actions.
```

---























